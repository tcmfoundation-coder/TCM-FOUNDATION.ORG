import { PlaceholderPage } from "@/components/placeholder-page";

export default function CallForApplicationDetailPage() {
  return (
    <PlaceholderPage
      title="Call for Application"
      description="Detail + apply form for a single campaign."
      phase="Phase 4"
    />
  );
}
